{
  "name": "workshop-submission-app",
  "version": "1.0.0",
  "description": "Workshop idea submission and results tracking system",
  "scripts": {
    "build": "mkdir -p dist && cp *.html dist/ && cp staticwebapp.config.json dist/",
    "start": "echo 'Static files served directly'"
  },
  "dependencies": {
    "@fluentui/react-components": "^9.72.4",
    "@fluentui/react-icons": "^2.0.313"
  }
}
